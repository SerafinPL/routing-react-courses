import React, { Component } from 'react';

import {withRouter} from 'react-router-dom';

const ErrorSite = () => (
	<h1> Nie odnaleziono storny</h1>
		);


export default ErrorSite;//withRouter(Course);